# esbuild's install script links the prebuilt binary vite needs, and core-js (pulled in by the
# TFJS toolchain) runs one too. pnpm requires build scripts to be allowed by name.
allowBuilds:
  esbuild: true
  core-js: true
